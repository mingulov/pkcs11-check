# Provider-Neutral Change Gap Analysis (2026-05-26)

This note records the review criteria for the current provider-matrix cleanup.
The goal is to keep pkcs11-check useful as a common test suite for arbitrary
PKCS#11 modules, not only for the software providers used in the Docker matrix.

## Decision Rules

- Spec checks should prefer a local OASIS TC checkout when available. In the
  current audit workspace, the neighboring checkout is `../other/pkcs11/`; use
  `published/3-02/` for released v3.2 headers and `working/doc/spec/` for
  searchable prose. The checkout's origin is
  `https://github.com/oasis-tcs/pkcs11.git`; the local HEAD observed during
  this audit was `48fa092`.
- Crashes, subprocess signal exits, hangs, accepted invalid cryptography, and
  failed valid cryptography remain findings. They must not be skipped or hidden.
- Missing advertised capability is a skip only when the mechanism or interface
  is absent. If a mechanism is advertised but fails at runtime, the result stays
  visible as a failure or xfail depending on whether the operation reached a
  meaningful provider outcome.
- Invalid-signature vectors have three outcomes:
  - clean PKCS#11 signature rejection (`CKR_SIGNATURE_INVALID` or
    `CKR_SIGNATURE_LEN_RANGE`) is a vector pass;
  - accepted invalid signature is a hard failure;
  - non-clean rejects such as `CKR_DATA_INVALID`, `CKR_DEVICE_ERROR`, or
    `CKR_FUNCTION_FAILED` are xfail evidence, not a clean pass.
- Duplicate vector skips are allowed only for exact PKCS#11-visible duplicates:
  if pkcs11-check has normalized away ASN.1, PEM, DER, P1363, JWK, WebCrypto, or
  ACVP seed metadata before calling the module, repeated identical module inputs
  should be counted as skipped duplicates rather than as repeated provider
  failures.
- Attribute-template quirks must not replace security tests. A mechanism
  roundtrip may retry a documented unwrap-template restriction only when the
  retried object is immediately used to prove the cryptographic roundtrip.
  Attribute enforcement and tamper/integrity tests stay strict.

## Current Change Review

| Area | Review result | Remaining risk |
| --- | --- | --- |
| Raw CKR subprocess reporting | Valid. It separates signal crashes from ordinary child-process assertion exits. It does not suppress crashes. | None beyond normal subprocess coverage. |
| Invalid-signature policy | Valid. It distinguishes cryptographic rejection from clean conformance. `CKR_DEVICE_ERROR` from Kryoptic-like behavior becomes xfail, not pass. | Reports and article text must not describe these rows as fully compliant. |
| EdDSA public-key encoding and runtime classification | Valid after focused rerun. The local OASIS spec tree requires raw RFC 8032 bytes for `CKK_EC_EDWARDS` `CKA_EC_POINT`, but current providers differ in practice. Vector tests now probe the provider profile with a known-good signature before running ACVP/Wycheproof rows, trying raw and DER-wrapped public points plus NULL and explicit `CKM_EDDSA` parameter modes. A dedicated `test_eddsa_public_key_encoding_support` row keeps the spec-facing result visible: raw support is clean, DER-only support is xfail evidence, and neither profile working is xfail evidence. Runtime rejects from unsupported curves remain skips. | Article wording must separate the standalone spec-facing encoding result from vector crypto coverage. The adaptive profile lets valid vectors run on DER-only or explicit-param providers, but the dedicated encoding test is the row that says whether the provider follows the raw `CKA_EC_POINT` convention. |
| EdDSA mechanism parameters | Valid after focused rerun. The helper no longer assumes a single provider interpretation of pure EdDSA: it probes NULL `CKM_EDDSA` and the raw helper's explicit `CK_EDDSA_PARAMS` form. NSS returned `CKR_FUNCTION_NOT_SUPPORTED` for the NULL profile and then worked with explicit params; Kryoptic worked with NULL params; SoftHSM2's focused result was dominated by point encoding rather than parameter mode. Only specific profile-reject CKRs are swallowed during probing. | Full matrix statistics still need a clean rerun, but the focused SoftHSM2/Kryoptic/NSS evidence is enough to avoid mislabeled EdDSA parameter-mode failures in the current article draft. |
| ACVP ML-KEM key generation runtime rejects | Valid after focused NSS-family rerun. `CKR_PARAMETER_SET_NOT_SUPPORTED` during ML-KEM-512 public-key import remains a capability skip, but `CKR_HOST_MEMORY` during advertised ML-KEM key generation is now xfail evidence rather than a hard harness failure. Focused current-source `nss`, `nss-main`, and `nss-pqc` runs all report 72 passed, 107 skipped, 1 xfailed, and 0 hard failures for `test_acvp_mlkem.py`. | The xfail is still a provider finding, not a clean pass. Full matrix statistics need a rerun before article tables use the reduced hard-failure count. |
| ACVP ECDH SPKI extraction and TPM2 import split | Reclassified after focused review. The repeated 1,403-row P-384/P-521 bucket was a pkcs11-check SubjectPublicKeyInfo parsing bug: the loader treated the first `0x04` byte as the EC point, but P-384/P-521 curve OIDs contain an earlier `0x04`. Current source parses the DER BIT STRING EC point explicitly. A current tpm2-pkcs11 focused rerun no longer has hard ACVP ECDH failures; vector rows stop at EC private-key import because tpm2-pkcs11 `C_CreateObject` does not import EC private keys, while the generated-key P-521 malformed `CKA_EC_POINT` row remains xfail evidence. | Do not use old ACVP ECDH hard-failure buckets as provider derive failures. Article wording should split: fixed loader issue, setup/import limitation where vector keys cannot be loaded, and advertised derive runtime rejects only when setup succeeds and `C_DeriveKey` rejects the valid operation. |
| TPM2 ACVP ECDSA KeyGen host-memory row | Reclassified after focused rerun. The retained full TPM2 artifact had one hard `CKR_HOST_MEMORY` row in `test_acvp_ecdsa.py::TestEcdsaKeyGen::test_ecdsa_keygen[ECDSA-KeyGen-P-521-tc19]`. Current source already classifies explicit EC runtime setup failures such as `CKR_HOST_MEMORY` as visible xfail evidence. A focused current-source rerun reports 31 passed, 67 skipped, 2 xfailed, and 0 failed in `artifacts/_focused/tpm2-acvp-ecdsa-current-20260526/`. | Do not use the old single ACVP ECDSA hard failure in article tables. It is stale after the setup/runtime classifier patch. |
| Wycheproof ECDH/XDH deduplication and decoder boundaries | Valid. Fingerprints include curve/OID, decoded public input, decoded private input, shared secret, and expected result. Exact duplicates are skipped after capability checks. ECDH decoder and curve-mapping handling now catches only expected vector decode/mapping errors; unexpected local bugs propagate instead of becoming provider capability skips. A focused pkcs11-mock ECDH rerun in `artifacts/_focused/pkcs11-mock-ecdh-decode-current-20260527/` reports 13,128 capability skips and 0 failures because the mock target does not advertise `ECDH1_DERIVE`. | Earlier Docker artifacts must be rerun before article statistics use the reduced buckets. The focused mock artifact is not provider ECDH behavior evidence; it only confirms counted capability skips after collection. |
| Wycheproof XDH runtime and decoder classification | Valid after regression. `test_wycheproof_x25519.py` now matches the ECDH split: private-key import rejects remain setup/capability skips, but valid-vector `CKM_ECDH1_DERIVE` rejects after a Montgomery key was imported are advertised-but-not-operational xfail evidence. Decoder handling now catches only expected vector decode failures (`ValueError`, `TypeError`, `KeyError`, `UnsupportedAlgorithm`, and base64/hex errors); unexpected decoder bugs propagate instead of becoming valid-vector skips or invalid-vector passes. A focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-xdh-decode-current-20260527/` reports 4,176 capability skips and 0 failures. | Current full artifacts mostly show XDH import/capability skips, not derive rejects. The old counts should not be interpreted as clean X25519/X448 support; they often mean the provider cannot import raw Montgomery private key bytes for the vector suite. Deriving a wrong shared secret, or deriving from malformed invalid public input that PKCS#11 actually sees, remains a hard failure. |
| Wycheproof ECDSA/DSA deduplication and digest normalization | Valid. PKCS#11 receives raw `r || s`, so DER/P1363 rows that normalize to the same public key, digest/message, signature, mechanism, and result are duplicate provider inputs. ECDSA fixed-width P1363 `SignatureSize` negatives are also not provider-neutral when the raw signature is a shorter even-length `r || s` form allowed by the current PKCS#11 spec. DSA public-key imports now also strip Wycheproof's ASN.1-style sign padding from `Big integer` attributes before calling `C_CreateObject`. ECDSA P-521/SHAKE256 now uses the Wycheproof-compatible 64-byte SHAKE256 output before raw `CKM_ECDSA` verification, rather than the 66-byte coordinate width. Focused SoftHSM2, NSS stable, and OpenCryptoki ECDSA reruns on current source now show 0 hard failures for the ECDSA file; OpenCryptoki reports 19,027 passed, 9,702 skipped, 186 xfailed, and 0 failed in `artifacts/_focused/opencryptoki-ecdsa-current-20260526/`. A current focused SoftHSM2 DSA rerun reports 613 passed, 1,343 skipped, and 0 failed in `artifacts/_focused/softhsm2-dsa-current-20260526/`. | Invalid vectors whose only invalidity was DER container metadata, Bitcoin low-S policy, fixed-width P1363 size policy, third-party integer container padding, or wrong harness XOF length must be described as loader-normalized or version-sensitive skips/fixes, not provider passes. Earlier matrix rows that still show ECDSA or DSA hard failures from these families need a refreshed full run before use in article tables. |
| Kryoptic Wycheproof ECDSA old bucket | Reclassified after focused rerun. Current-source Kryoptic `test_wycheproof_ecdsa.py` reports 5,820 passed, 22,083 skipped, 1,012 xfailed, and 0 failed in `artifacts/_focused/kryoptic-ecdsa-current-20260526/`. The xfails are all invalid signatures rejected with Kryoptic's documented non-clean `CKR_DEVICE_ERROR`, not accepted invalid signatures. | Do not use the old 467-row Kryoptic ECDSA hard-failure bucket in article tables. Also do not describe the current row as fully clean ECDSA compliance; invalid-signature rejection still uses a non-spec CKR. |
| Wycheproof AES-KWP mechanism selection | Reclassified as a pkcs11-check bug after focused reruns. Wycheproof `aes_kwp_test.json` vectors are RFC 5649 raw-data KWP vectors, but `test_wycheproof_aes.py` used deprecated `CKM_AES_KEY_WRAP_PAD` with `C_WrapKey`. The local OASIS spec tree says `CKM_AES_KEY_WRAP_KWP` is the RFC 5649 mechanism and `CKM_AES_KEY_WRAP_PAD` is deprecated. Current source now requires `AES_KEY_WRAP_KWP` and uses `C_Encrypt` on the raw vector input, matching the ACVP KWP test shape. Focused current-source `nss` and `nss-pqc` reruns both report 724 passed, 1,095 skipped, and 0 failed for `test_wycheproof_aes.py`; a focused OpenCryptoki rerun reports 726 passed, 1,013 skipped, 80 xfailed, and 0 failed, with the xfails all from AES-XTS runtime rejects. | Do not use old NSS or OpenCryptoki Wycheproof AES/KWP buckets as provider failures. Existing AES-KWP provider findings should be based on tests that call `CKM_AES_KEY_WRAP_KWP`, not the deprecated PAD mechanism. Focused evidence is in `artifacts/_focused/nss-wycheproof-aes-after-kwp-fix-20260526/`, `artifacts/_focused/nss-pqc-wycheproof-aes-after-kwp-fix-20260526/`, and `artifacts/_focused/opencryptoki-wycheproof-aes-after-kwp-fix-20260526/`. |
| Wycheproof RSA/RSA-PSS deduplication and integer normalization | Valid. Fingerprints include mechanism, mechanism parameters where applicable, public key, message, and signature. RSA public/private key imports now strip third-party ASN.1-style sign padding from PKCS#11 `Big integer` attributes, matching the same rule used for DSA. | Need refreshed Docker rows because this patch landed after several targets had already started. RSA PKCS#1 decrypt rows where invalid ciphertext is accepted remain provider/security findings, not loader-padding artifacts. |
| TPM2 Wycheproof RSA-PSS remaining failures | Valid provider findings after focused rerun. Current-source `test_wycheproof_rsa_pss.py` on tpm2-pkcs11 1.10.0 still reports 82 hard failures: 43 valid signatures rejected and 39 invalid signatures accepted. The invalid accepted rows are salt-length mutation vectors; a control check verifies they fail with the requested `CK_RSA_PKCS_PSS_PARAMS.sLen` and pass only when verification uses automatic salt length. tpm2-pkcs11 source validates PSS params but the software RSA verify path sets padding and digest without setting PSS salt length or MGF digest on the OpenSSL verification context. | Keep these as hard failures. They are not duplicate-vector noise, ASN.1 integer padding, or a PKCS#11 expressiveness gap. The SHA-1 valid-vector rejects are separately reportable advertised-mechanism behavior. Focused evidence is in `artifacts/_focused/tpm2-rsapss-current-20260526/`. |
| TPM2 ACVP RSA SHA-1 PKCS#1 SigVer failures | Valid provider findings after focused rerun. Current-source `test_acvp_rsa.py` on tpm2-pkcs11 1.10.0 reports 279 passed, 390 skipped, 194 xfailed, and 27 failed in `artifacts/_focused/tpm2-acvp-rsa-current-20260526/`. Every hard failure is a valid ACVP `CKM_SHA1_RSA_PKCS` SigVer row rejected by the provider. The representative vectors verify with `cryptography`, including FIPS186-2 rows with small public exponents and FIPS186-4 rows with larger exponents. | Keep these as hard failures. The PSS parameter rejects in the same file are already visible xfail evidence, but valid SHA-1 PKCS#1 verification returning false is a semantic failure after setup succeeds. |
| NSS-family mechanism crash findings | Valid provider crash findings after focused reruns. Current-source `test_mech_flags.py` crashes all NSS-family targets on `C_SignInit(CKM_AES_MAC_GENERAL, key=0)` in `test_sign_flag_callable[AES_MAC_GENERAL]`. Current-source `test_mech_negative.py` crashes `nss-main` and `nss-pqc` on `C_SignInit(CKM_SHA256_HMAC, RSA private key)` in `test_hmac_sha256_with_rsa_key_rejected`. The runner first observes the file-level segfault, then adaptive isolation reruns the single node and records it as `crashed` in `results.json`. | Keep these as crash findings. The parameters are intentionally invalid/negative, but a PKCS#11 provider must return a CKR rather than segfaulting. No skip or xfail should be added; the existing mixed-isolation runner already makes the crash discoverable. Focused evidence is in `artifacts/_focused/nss-mech-flags-current-20260526/`, `artifacts/_focused/nss-main-mech-flags-current-20260526/`, `artifacts/_focused/nss-pqc-mech-flags-current-20260526/`, `artifacts/_focused/nss-main-mech-negative-current-20260526/`, and `artifacts/_focused/nss-pqc-mech-negative-current-20260526/`. |
| Wycheproof RSA PKCS#1 decrypt runtime classification | Valid after focused reruns. Current source keeps accepted invalid ciphertext as a hard failure, but treats valid-vector `C_Decrypt` runtime CKRs from an advertised `CKM_RSA_PKCS` path as visible xfail evidence. The pkcs11-mock focused run moved its old RSA decrypt hard bucket to 77 passed, 124 xfailed, and 0 failed after adding `CKR_KEY_TYPE_INCONSISTENT` to that valid-vector runtime-reject set. A current focused SoftHSM2 rerun still reports 142 passed and 59 failed in `artifacts/_focused/softhsm2-rsa-decrypt-current-20260526/`; all 59 failures are invalid PKCS#1 padding ciphertexts accepted after `CKR_OK`, split across 2048/3072/4096-bit files. | This does not make RSA PKCS#1 decrypt compliance clean. Article wording should call valid-vector runtime CKRs advertised-but-not-operational evidence, and should keep accepted-invalid-ciphertext rows as provider/security findings. |
| Wycheproof RSA-OAEP runtime classification | Valid after focused pkcs11-mock rerun. Current source now treats `CKR_KEY_TYPE_INCONSISTENT` like the other explicit valid-vector RSA-OAEP runtime rejects from an advertised `CKM_RSA_PKCS_OAEP` path. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-rsa-oaep-current-20260526/` moved the old 700 hard failures to 373 passed, 700 xfailed, and 0 failed. | This is not a clean RSA-OAEP pass for pkcs11-mock. It means the mock advertises/reaches OAEP but rejects valid decrypt operations with an explicit CKR. Wrong plaintext after `CKR_OK`, accepted invalid ciphertext, crashes, and timeouts remain hard failures. |
| RSA-OAEP hash-combo runtime rejects | Valid after focused SoftHSM2 rerun. The dedicated `test_rsa_oaep.py` SHA-384/SHA-512 rows now classify explicit advertised-mechanism parameter/runtime rejects as visible xfail evidence instead of raw failures. The current SoftHSM2 focused file run reports the SHA-1 compatibility rows as passed and the SHA-384/SHA-512 rows as xfailed. | This is not a clean full-OAEP result. Article wording should say SoftHSM2's compatibility OAEP profile works for SHA-1/MGF1-SHA1, while broader OAEP hash/MGF combinations remain non-operational evidence. Wrong plaintext, deterministic OAEP ciphertext, accepted wrong-key decrypt, crashes, or timeouts remain hard failures. |
| ACVP RSA-PSS SHAKE mask-function rows | Reclassified after focused OpenCryptoki rerun. ACVP FIPS186-5 RSA-PSS groups can use `maskFunction` values such as `shake-128` or `shake-256`, but PKCS#11 `CK_RSA_PKCS_MGF_TYPE` exposes only `CKG_MGF1_SHA*` / `CKG_MGF1_SHA3_*` constants in the local v3.2 headers. The loader now collects only missing or `mgf1` PSS mask-function rows and preserves the source `mask_function` metadata. A focused OpenCryptoki ACVP RSA rerun on current source reported 890 passed and 0 failed for the file. | Do not count the old six SHA3-256 PSS valid-signature rejects as provider failures. Those rows were pkcs11-check sending `MGF1-SHA3-256` for ACVP vectors that specified SHAKE mask functions. Article wording should say these ACVP rows are outside current PKCS#11 RSA-PSS parameter expressiveness unless a future standard adds SHAKE MGF constants. |
| Kryoptic RSA signature buckets | Valid after focused rerun. Current-source focused runs of Wycheproof RSA-PSS and RSA PKCS#1 signature vectors now have 0 hard failures for Kryoptic. The remaining large counts are duplicate skips and xfails where Kryoptic rejects invalid or acceptable signatures with `CKR_DEVICE_ERROR`; that is non-clean provider evidence, not a pkcs11-check vector-loader failure. | Article tables must not use the old Kryoptic RSA-PSS/RSA hard-failure buckets without a refreshed matrix. The xfail counts should be described as non-clean invalid-signature rejection, not clean pass. |
| Kryoptic ACVP AES-CTS | Valid after focused rerun. The old hard-failure bucket is now visible xfail evidence: selected CTS variant rows reach advertised `CKM_AES_CTS` and return `CKR_DEVICE_ERROR`, while matching rows that complete are still checked against exact ciphertext/plaintext and non-selected CS variants are skipped. Kryoptic source maps `CKM_AES_CTS` to CTS mode CS1, and pkcs11-check detects the module variant before selecting vectors. Focused rerun evidence is stored in `artifacts/_focused/kryoptic-cts-current-20260526/`. | This is not a clean CTS pass. Current source reports non-operational runtime rows as xfail evidence, while wrong ciphertext/plaintext after `CKR_OK`, crashes, and timeouts remain hard failures. Full matrix artifacts generated before the classifier patch still contain stale hard failures for this file. |
| ACVP KeyGen deduplication | Valid with a standards caveat. Current PKCS#11 key-generation APIs do not accept ACVP internal-projection seeds, so only the provider-visible parameter set can be tested. | Documentation must continue saying this is not an exact ACVP KeyGen reproduction. |
| OpenCryptoki ACVP/FFI ML-DSA | Partially reclassified after focused reruns. Non-empty-context SigGen/Wycheproof failures were a pkcs11-check context-propagation bug and are fixed by passing `CK_SIGN_ADDITIONAL_CONTEXT` into the follow-up verify call. Context-empty ACVP SigVer valid-signature rejects remain provider findings: the local OASIS ML-DSA text says absent parameters mean `CKH_HEDGE_PREFERRED`, `ulContextLen=0`, and `pContext=NULL`, and the data passed to `CKM_ML_DSA` is the message `M`. A new subprocess FFI probe also keeps the OpenCryptoki swtok abort for explicit non-NULL empty context discoverable. | Do not describe the old ML-DSA bucket as one uniform signing failure. Article/report wording should split fixed harness context propagation, remaining valid-signature rejects, non-clean invalid-signature xfails, and the separate explicit-empty-context abort. |
| OpenCryptoki ACVP AES-XTS | Reclassified after focused rerun. The old hard-failure/xfail bucket was a pkcs11-check ACVP vector-shape bug, not a confirmed OpenCryptoki XTS interop bug. ACVP XTS number-mode vectors use `sequenceNumber`, while PKCS#11 `CKM_AES_XTS` expects a 16-byte data-unit sequence number parameter. The loader also lost group-level `payloadLen`, so bit-level ACVP vectors were sent through a byte-string API. Current source preserves payload/data-unit lengths, converts `sequenceNumber` to the little-endian 16-byte tweak, chunks multi-data-unit vectors, and skips non-byte-aligned vectors at runtime. Focused rerun evidence is stored in `artifacts/_focused/opencryptoki-xts-after-loader-fix-20260526/`. | Full matrix artifacts generated before this patch still contain the stale `ACVP AES-XTS` bucket. The skipped focused rows are ACVP bit-level vectors that PKCS#11 cannot express as byte-string `CKM_AES_XTS` inputs, with both non-byte-aligned `payloadLen` and non-byte-aligned `dataUnitLen` cases present in the artifact. |
| OpenCryptoki generic Wycheproof AES-CBC-PAD | Valid provider/security finding after focused rerun. The generic Wycheproof AES-CBC-PAD test sends `CKM_AES_CBC_PAD` decrypt inputs directly from `aes_cbc_pkcs5_test.json`; current OpenCryptoki still returns plaintext instead of rejecting 144 invalid padding vectors across all three AES key sizes. | Keep this as accepted-invalid-input evidence. It is not similar to the ECDSA/ECDH normalization fixes because no container metadata is stripped before the provider sees the ciphertext and IV. |
| OpenCryptoki unwrap-template fallback | Conditionally valid. The OASIS PKCS#11 specification `C_UnwrapKey` section allows `CKA_CLASS` and `CKA_KEY_TYPE` in unwrap examples, and says unsupported precise templates fail (<https://docs.oasis-open.org/pkcs11/pkcs11-spec/v3.2/pkcs11-spec-v3.2.html>). For mechanism-level roundtrips, retrying without these identifiers is useful only if the unwrapped key is then verified cryptographically and a compliance note is emitted. | Pass counts alone can overstate compliance. Results should include compliance notes or a separate "template quirk retried" note for OpenCryptoki. |
| NSS X448 and HKDF lifecycle classification | Valid after focused rerun. `CKR_DOMAIN_PARAMS_INVALID` on the X448 keygen row is a missing-curve capability skip, not a mechanism-level failure, because NSS still supports X25519. HKDF lifecycle now classifies `CKM_HKDF_KEY_GEN` setup rejects such as `CKR_MECHANISM_INVALID` as advertised-but-not-operational xfail evidence. A current-source NSS focused run of ECDH extended, HKDF extended/basic/lifecycle, Wycheproof X25519/X448, and Wycheproof HKDF has 0 failed and 0 crashed. | This is not a clean HKDF implementation result: the HKDF lifecycle row is still xfail evidence. Article wording should distinguish skipped X448 curve support from HKDF advertised-runtime rejects. |
| Kryoptic HKDF generated-key value readback | Valid after focused rerun. Kryoptic `CKM_HKDF_KEY_GEN` can generate a `CKK_HKDF` key that remains usable for HKDF derive, but reading `CKA_VALUE` from the generated key returns `CKR_ATTRIBUTE_VALUE_INVALID` even though the test requested `CKA_SENSITIVE=False` and `CKA_EXTRACTABLE=True`. Current source classifies only that explicit generated-key value-readback CKR as xfail evidence. Key-generation rejects, wrong `CKA_KEY_TYPE`, wrong value length after successful readback, and derive output checks remain strict. | This is not a clean HKDF pass. Article wording should distinguish usable HKDF derivation from the generated-key extractability/readback quirk. Older Kryoptic rows that show this as a hard `test_hkdf_extended.py` failure are stale on current source. |
| KDF derive catch boundaries | Valid after regression. Basic HKDF, HKDF extended, miscellaneous KDF, and SP800-108 KDF runtime classifiers now only convert PKCS#11 `AssertionError`/CKR paths into xfail evidence. Python-side bugs whose messages happen to contain a CKR name propagate as real failures instead of becoming provider xfails. | Other older object/profile/domain tests still contain broad compatibility catches and should be narrowed only with focused evidence and regression coverage. |
| Object metadata catch boundaries | Valid after regression. Trust, validation, profile, and EC domain-parameter metadata tests no longer catch arbitrary Python `Exception` before converting object-enumeration or attribute-read failures into skip/xfail/optional-attribute behavior. Only PKCS#11 `AssertionError`/CKR paths are classified. A synthetic trust-object read bug containing `CKR_FUNCTION_FAILED` now propagates as the original Python failure instead of becoming provider xfail evidence. | The object tests still intentionally skip absent object classes and optional attributes. That is capability/metadata presence behavior, not proof that a provider implements those optional object families. Older X.509/object tests outside this scoped patch still deserve gradual audit when touched. |
| X.509 metadata catch boundaries | Valid after regression. X.509 attribute parity, core certificate attribute reads, Limbo-derived attribute/search checks, identity import/sign flows, and lifecycle metadata probes now only treat PKCS#11 `AssertionError` paths as provider capability/attribute behavior. Python-side read or parsing bugs no longer become missing attribute parity, expected import rejection, skipped certificate support, or silent optional-field absence. The certificate-support probe also no longer converts local certificate-construction errors into "module does not support X.509". A current focused pkcs11-mock Limbo rerun in `artifacts/_focused/pkcs11-mock-x509-limbo-current-20260526/` reports 1,178 hard failures, all from accepted certificate objects whose `CKA_VALUE` readback is the mock placeholder rather than the supplied DER. | `pem_to_der()` still returns `None` for local PEM/base64 decode failures; callers already skip those rows as un-decodable test input, not provider findings. The pkcs11-mock rows are a mock storage finding, not crash evidence; article tables should label pkcs11-mock separately from real providers. |
| Attribute date catch boundaries | Valid after regression. `CKA_START_DATE` / `CKA_END_DATE` setup and readback tests now only convert PKCS#11 `AssertionError`/CKR paths into date-attribute capability skips. Python-side setup/read bugs whose messages include CKR names now propagate as real failures. Existing SoftHSM2 and TPM2 artifact date skips should therefore be treated as provider CKR evidence only after rerun on current source. | This does not make date attributes mandatory across every provider; the tests still skip explicit CKR rejects or missing date attributes. It only prevents harness exceptions from being counted as provider capability gaps. |
| CKR wrap capability and quirk guards | Valid after regression and focused reruns. `test_ckr_wrap.py::TestWrapKeyErrors::test_mechanism_invalid` now applies the same `AES_KEY_WRAP` capability guard as the other wrap tests before generating AES setup keys, so TPM2/pkcs11-mock targets without `AES_KEY_WRAP` become capability skips rather than setup failures. The same test now validates the returned CKR through the CKR spec table instead of merely checking `rv != CKR_OK`. `test_wrapping_key_size_range` also applies the existing documented SoftHSM2 `size_range_on_wrap` quirk in compatibility mode, while strict CKR mode still requires the spec CKR. Focused current-source reruns show SoftHSM2 6 passed / 0 failed, TPM2 6 skipped / 0 failed, and pkcs11-mock 6 skipped / 0 failed. | Old `test_ckr_wrap.py` hard failures for SoftHSM2, TPM2, and pkcs11-mock are stale. Kryoptic/NSS wrap-security failures remain real provider findings unless a focused rerun shows otherwise. Focused evidence is in `artifacts/_focused/softhsm2-ckr-wrap-current-20260526/`, `artifacts/_focused/tpm2-ckr-wrap-current-20260526/`, and `artifacts/_focused/pkcs11-mock-ckr-wrap-current-20260526/`. |
| Buffer-size digest/sign capability guards | Valid after regression and focused pkcs11-mock/TPM2 reruns. The simple buffer-size SHA-256 digest tests now skip when `CKM_SHA256` is not advertised instead of calling `C_DigestInit` unguarded. The simple RSA/SHA-256 sign buffer tests now use the same `SHA256_RSA_PKCS` and `RSA_PKCS_KEY_PAIR_GEN` guards, plus the shared RSA keypair setup classifier, as the explicit output-buffer edge test. The current pkcs11-mock batch no longer shows the old setup failures; its remaining `test_buffers.py` hard failures are three AES imported-key value readbacks returning the mock placeholder `Hello world!` instead of the requested key bytes. Current TPM2 reports 22 passed, 5 xfailed, and 0 failed for this file. | Wrong imported-key readback, wrong ciphertext/plaintext, and buffer-size writeback/state-preservation failures remain provider findings. BouncyHSM rows still need current reruns before final hard-failure counts are used. |
| General digest capability/runtime classifiers | Valid after regression and focused pkcs11-mock/TPM2 reruns. `test_digest.py` now guards each SHA-family digest row with the advertised digest mechanism, reports explicit runtime rejects from advertised digest operations as xfail evidence, and classifies AES setup rejects before `C_DigestKey` as setup xfail evidence. `C_DigestKey` returning `CKR_FUNCTION_NOT_SUPPORTED` remains a clean skip because the function is optional for many modules. The current pkcs11-mock batch leaves one hard SHA-1 digest mismatch after `CKR_OK`, which is a mock crypto result finding rather than setup noise. Current TPM2 reports 15 passed, 2 skipped, 3 xfailed, and 0 failed for this file. | Digest byte mismatches after `CKR_OK`, wrong output lengths after `CKR_OK`, and `C_DigestKey` mismatches remain hard failures. BouncyHSM digest rows still need a current rerun before final hard-failure counts are used. |
| Standalone KAT AES-ECB classifiers | Valid after regression and focused pkcs11-mock rerun. `test_kat.py` now checks `AES_ECB` before AES KAT key import and classifies advertised AES KAT key-import/encrypt/decrypt CKR rejects as visible xfail evidence. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-kat-r2-20260527/` reports 2 failed, 18 skipped, and 1 xfailed, replacing the previous 10 failed / 10 skipped / 1 xfailed slice. The remaining failures are SHA-1 digest byte mismatches after `CKR_OK`. | The old pkcs11-mock standalone AES KAT rows were missing-capability artifacts because the mock does not advertise `CKM_AES_ECB`. This does not suppress real KAT failures: wrong AES ciphertext/plaintext after `CKR_OK`, wrong digest bytes, crashes, and timeouts remain hard failures. |
| Legacy sign/key-management classifiers | Valid after regression and focused pkcs11-mock rerun. `test_sign.py` now checks RSA sign mechanisms before RSA setup and classifies advertised RSA sign/verify operation rejects as visible xfail evidence. `test_keymgmt.py` now guards AES-ECB roundtrip/copy paths and uses shared AES/RSA/EC setup classifiers. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-sign-keymgmt-r2-20260527/` moved from 2 passed / 14 failed / 12 skipped to 2 passed / 4 failed / 22 skipped. | The four remaining pkcs11-mock failures are strict key-management readback findings: imported AES `CKA_KEY_TYPE` reads back empty, extractable imported AES values read back `Hello world!`, and generated RSA public modulus reads back empty. Wrong signatures, false verification, bad key export/readback, crashes, and timeouts remain hard failures. |
| General error-path setup classifiers | Valid after regression and focused pkcs11-mock/TPM2 reruns. `test_errors.py` now guards AES/SHA/RSA preconditions before entering general negative and edge-case rows. Missing AES/SHA/RSA mechanisms skip before setup, while advertised AES/RSA setup rejects such as TPM2-style `CKR_FUNCTION_NOT_SUPPORTED` / `CKR_ATTRIBUTE_VALUE_INVALID` become visible setup xfail evidence. The invalid AES key-size test still accepts real key-size/template/argument CKRs as the target negative outcome; only an advertised function-unavailable result is xfail evidence. The current pkcs11-mock batch reports 6 passed, 11 skipped, and 0 failed; current TPM2 reports 9 passed, 8 xfailed, and 0 failed for this file. | This does not suppress actual negative-test findings. Accepted invalid parameters, wrong key-use behavior, successful use of destroyed keys, digest/sign/encrypt output mismatches, and crashes/timeouts remain failures. |
| Access-level extra-session capacity | Valid after regression and focused pkcs11-mock/TPM2 reruns. The pkcs11-mock `test_access_levels.py` failures in the public-session digest/random and public-session creation rows stopped before the access rule was tested: opening an additional public/RW session returned `CKR_SESSION_COUNT`. Current source treats only that exact extra-session setup limit as a skip, while all access-control assertions, operation CKRs, crashes, and other session-open errors still propagate. The current pkcs11-mock batch reports all 27 access-level rows skipped and 0 failed; current TPM2 reports 5 passed, 8 skipped, 14 xfailed, and 0 failed. | This does not reclassify real access-control findings such as private objects visible in a public session, USER-created `CKA_TRUSTED=True` keys, USER escalation of `CKA_TRUSTED`, or untrusted wrapping of `CKA_WRAP_WITH_TRUSTED` targets. |
| Legacy access test setup classifiers | Valid after regression and focused pkcs11-mock rerun. The older `test_access.py` file had the same setup split as the newer access/session files: pkcs11-mock does not advertise `AES_KEY_GEN`, and attempts to open an extra session stop at `CKR_SESSION_COUNT` before the access rule is reached. Current source now skips only those exact setup limits and classifies advertised AES/RSA setup rejects as visible xfail evidence. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-access-r2-20260527/` reports 1 passed, 7 skipped, and 0 failed. | This does not weaken the access assertions. If setup succeeds, private-object visibility, cross-session visibility, session-object lifetime, wrong CKRs, crashes, and timeouts remain hard failures. AES setup keys are AES-128 because these rows test access/session semantics, not AES-256 support. |
| API security setup classifiers | Valid after regression and focused pkcs11-mock rerun. `security/test_api_security.py` no longer treats missing AES key generation as successful prevention of the wrap-decrypt oracle, and the AES/RSA fixture keys plus extra public session now use the same skip/xfail setup boundary as the rest of the access/security suite. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-api-security-r2-20260527/` reports 11 setup skips and 1 hard failure. | The remaining pkcs11-mock hard row is not setup noise: after RSA keypair setup succeeds, `CKA_PRIVATE_EXPONENT` is returned as a readable attribute. Security-policy successes, key-material readback, attribute escalation, handle predictability, crashes, and timeouts remain strict. |
| CVE regression capability guards | Valid after regression and focused pkcs11-mock/TPM2 reruns. The old `test_cve_regression.py` provider bucket mixed real security findings with setup failures. Current source now skips AES-ECB boundary testing when `AES_ECB`/`AES_KEY_GEN` is absent, xfails advertised-but-rejected AES key generation, skips the rapid RSA signing deadlock probe when `SHA256_RSA_PKCS` or RSA key generation is absent, xfails advertised-but-rejected RSA setup, and classifies AES setup rejection in the session-object-after-logout row before checking logout semantics. The Tookan §3.3 sensitive-unwrap row now skips only explicit protective sensitive-key wrap refusals such as `CKR_KEY_NOT_WRAPPABLE` / `CKR_KEY_FUNCTION_NOT_PERMITTED` / `CKR_ACTION_PROHIBITED`; generic wrap runtime CKRs are xfail evidence instead of skips. The current pkcs11-mock batch reports 5 passed, 8 skipped, and 0 failed; current TPM2 reports 8 passed, 3 skipped, 2 xfailed, and 0 failed for this file. Focused pkcs11-mock evidence for the narrowed Tookan wrap policy is in `artifacts/_focused/pkcs11-mock-tookan-sensitive-wrap-current-20260527/` with 1 skipped and 0 failed. | Tookan unwrap downgrades, extractable escalation, accepted invalid boundary input, timing variance, crashes, and deadlocks remain findings. |
| Tookan key-type-confusion wrap precondition | Valid after regression and focused pkcs11-mock rerun. `security/test_tookan.py::TestKeyTypeConfusionOnUnwrap` now applies the same wrap-side split as the §3.3 sensitive-unwrap row: explicit protective wrap refusals skip because the attack cannot reach unwrap, while generic runtime CKRs from advertised `CKM_AES_KEY_WRAP` become xfail evidence. Focused evidence in `artifacts/_focused/pkcs11-mock-tookan-type-confusion-current-20260527/` reports 1 skipped and 0 failed. | If wrapping succeeds, the unwrap type-confusion assertion remains strict: accepted AES-as-DES3 unwraps, readable confused key bytes, crashes, and timeouts remain findings. |
| Destroyed-handle reuse checks | Valid after regression and focused pkcs11-mock rerun. `security/test_handle_reuse.py` no longer swallows arbitrary exceptions or treats any non-OK CKR as good enough. Missing AES/RSA/wrap mechanisms skip before setup, setup keys use the shared classifiers, successful use of a destroyed handle fails, Python-side readback bugs propagate, and destroyed-handle operations now accept only explicit handle-related CKRs. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-handle-reuse-r2-20260527/` moved from 1 passed / 5 failed / 1 skipped to 7 skipped. | This change makes the test stricter where setup succeeds: `CKR_MECHANISM_INVALID` or another unrelated CKR from a destroyed-handle operation is a failure, not a pass. Crashes and timeouts remain findings. |
| Mechanism lifecycle setup classifiers | Valid after regression. Composite lifecycle tests now use shared AES, RSA, and EC setup classifiers before the intended lifecycle operation. TPM2-style `CKR_FUNCTION_NOT_SUPPORTED` from advertised AES key generation and `CKR_ATTRIBUTE_VALUE_INVALID` from EC/RSA keypair setup become visible xfail evidence instead of raw harness failures. The existing NSS HKDF keygen classifier remains in place. | This does not hide lifecycle semantic failures after setup succeeds: wrong derived-key output, wrap/unwrap mismatches, RSA-OAEP parameter rejects, digest/encrypt mismatches, and operation crashes remain findings. Old `test_mech_lifecycle.py` rows should be rerun before final provider tables use the reduced hard-failure count. |
| Mechanism-driven sign setup/runtime classifiers | Valid after regression and focused pkcs11-mock/TPM2 reruns. `test_mech_sign.py` now uses the shared mechanism helper split for advertised sign mechanisms: absent key-generation mechanisms skip, explicit runtime rejects from advertised key generation or keypair generation xfail, explicit sign/verify runtime rejects xfail, and tampered-signature verification uses the shared invalid-signature policy. The old BouncyHSM BLAKE2 HMAC `CKR_TEMPLATE_INCONSISTENT` rows are stale hard-failure artifacts on current source. The current pkcs11-mock batch leaves two hard RSA tampered-signature failures where verify returns true after setup succeeds; current TPM2 reports 1 passed, 23 skipped, 36 xfailed, and 0 failed. | Real sign findings remain failures: accepted tampered signatures, valid sign/verify returning false after setup succeeds, deterministic KAT mismatches after `CKR_OK`, wrong MAC/signature bytes, crashes, and timeouts are not suppressed. BouncyHSM rows still need a current rerun before article tables use reduced hard-failure counts. |
| Standalone multipart streaming classifiers | Valid after regression and focused pkcs11-mock/TPM2 reruns. `test_multipart_streaming.py` now applies explicit mechanism guards before AES, SHA-2, RSA, and HMAC smoke paths; AES setup keys use AES-128 because these rows test large-buffer streaming behavior rather than AES-256 key generation; imported AES/HMAC keys declare `CKA_ALLOWED_MECHANISMS` for the mechanism under test; HMAC setup tries typed HMAC and generic-secret key types; and advertised-but-rejected setup or valid runtime operations become visible xfail evidence, including `CKR_BUFFER_TOO_SMALL` during an advertised HMAC sign path. The current pkcs11-mock batch reports all 20 rows skipped and 0 failed; current TPM2 reports 8 passed, 12 xfailed, and 0 failed. | Plaintext/ciphertext, digest, MAC, signature, and verification mismatches after `CKR_OK` remain hard failures. BouncyHSM rows from this file still need a current rerun before final hard-failure counts are used. |
| Legacy multipart smoke classifiers | Valid after regression and focused pkcs11-mock rerun. `test_multipart.py` now checks required digest/AES/RSA mechanisms before operation setup, uses AES-128 setup keys for the AES payload smoke rows, and classifies advertised setup or operation CKR rejects as visible xfail evidence. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-multipart-r2-20260527/` reports 9 skipped and 0 failed instead of the previous 9 hard `CKR_MECHANISM_INVALID` rows. | This file is a legacy smoke test, not the mechanism-driven multipart matrix. Wrong digest/ciphertext/plaintext/signature results after `CKR_OK`, false verification, crashes, and timeouts remain hard failures. |
| Session state-machine setup classifiers | Valid after regression and focused pkcs11-mock/TPM2 reruns. `test_session_state_machine.py` now treats `CKR_SESSION_COUNT` while opening an extra session as a capacity skip, and classifies AES/data-object setup rejects as visible xfail evidence before the intended login/logout/session-visibility assertion. Setup AES keys use 128-bit keys because these rows test session state, not AES-256 support. The current pkcs11-mock batch reports all 32 rows skipped and 0 failed. Current TPM2 reports 17 passed, 2 skipped, 12 xfailed, and 1 hard failure: private keys are visible in a public session before login. | Real state-machine findings remain failures: private objects visible without login or after logout, login state not shared across sessions, RO-session semantic rejects, and stale-session behavior are not suppressed. |
| RO-session setup classifiers | Valid after regression and focused pkcs11-mock/TPM2 reruns. `test_ro_session_restrictions.py` now treats `CKR_SESSION_COUNT` while opening an extra RO session as a capacity skip and classifies advertised-but-rejected AES, generic-secret, and RSA setup key generation as visible xfail evidence before the intended RO-session assertion. Negative RO token-object key generation still requires a RO error after setup reaches `C_GenerateKey`; setup rejects such as `CKR_FUNCTION_NOT_SUPPORTED` are xfail evidence, not pass. The current pkcs11-mock batch reports all 18 rows skipped and 0 failed. Current TPM2 reports 5 passed, 1 skipped, 10 xfailed, and 2 hard failures where session-object secret-key creation and session-keypair generation in RO sessions return `CKR_SESSION_READ_ONLY`. | Real RO-session semantic findings remain failures. Providers that reject session-object creation or session-key crypto in RO sessions with `CKR_SESSION_READ_ONLY` are still reported, because PKCS#11 permits session-object creation in RO sessions. BouncyHSM rows from this file still need a current rerun before final hard-failure counts are used. |
| Object-visibility setup classifiers | Valid after regression and focused pkcs11-mock/TPM2 reruns. `test_object_visibility.py` now treats `CKR_SESSION_COUNT` while opening an additional visibility-check session as a capacity skip, classifies advertised-but-rejected AES setup key generation as xfail evidence, and classifies CKO_DATA setup rejects such as `CKR_ATTRIBUTE_VALUE_INVALID` / template errors as xfail evidence before the intended visibility assertion. AES setup keys use 128-bit keys because these rows test object lifetime and visibility, not AES-256 support. The current pkcs11-mock batch reports 2 passed, 21 skipped, and 0 failed. Current TPM2 reports 12 passed, 9 xfailed, and 2 hard failures where session objects survive their owning session close. | Real object-visibility findings remain failures: session objects surviving their owning session close, token/session objects not appearing where required, destroyed objects remaining visible, private-object visibility leaks, and attribute-value mismatches after successful setup are not suppressed. |
| Object attribute/setup classifiers | Valid after regression and focused pkcs11-mock/TPM2 reruns. `test_object.py` now uses the shared AES/RSA/EC setup classifiers before object-label, object-search, keypair, and RSA public-key import-copy rows. Missing setup mechanisms skip; advertised key-generation/import rejects xfail as setup evidence; generated RSA public attributes are validated before being reused as import input. The current pkcs11-mock batch leaves five hard object readback failures after setup succeeds: empty RSA class/modulus/exponent attributes, empty AES key type, and `CKA_VALUE` returning the mock placeholder instead of requested bytes. Current TPM2 reports 10 passed, 6 xfailed, and 0 failed. | Dedicated object readback checks remain strict. Blank/wrong `CKA_CLASS`, `CKA_KEY_TYPE`, `CKA_MODULUS`, `CKA_PUBLIC_EXPONENT`, `CKA_VALUE`, failed find/destroy behavior, and verify failures after successful setup remain hard failures. Focused evidence is in `artifacts/_focused/pkcs11-mock-setup-classifiers-current-20260526/` and `artifacts/_focused/tpm2-setup-classifiers-current-20260526-r3/`. |
| Key-size RSA sign capability guards | Valid after regression and focused pkcs11-mock rerun. `test_key_sizes.py` now checks `SHA256_RSA_PKCS` before RSA sign/verify key-size rows and classifies advertised sign/verify CKR rejects as visible xfail evidence. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-key-sizes-r2-20260527/` reports 2 passed, 9 skipped, and 6 failed, replacing the previous 2 passed, 6 skipped, and 9 failed slice. | The remaining six pkcs11-mock failures are real readback/value findings after setup succeeds: imported AES keys read back as `Hello world!`, and generated RSA public moduli read back as empty bytes. Wrong sign/verify output after `CKR_OK`, false verification, crashes, and timeouts remain hard failures. |
| Metamorphic signature and digest guards | Valid after regression and focused pkcs11-mock rerun. `test_metamorphic.py` no longer catches its own `assert result is False` for RSA wrong-data verification, so accepting a tampered signature is a hard failure again. The same file now checks `SHA256_RSA_PKCS`, `SHA256`, `SHA_1`, and `SHA512` before RSA/digest invariants, so missing mechanisms are counted skips before setup. The focused pkcs11-mock rerun moved from 6 failed / 9 skipped to 15 skipped in `artifacts/_focused/pkcs11-mock-metamorphic-signature-current-r2-20260527/`. | This does not weaken the invariant checks. If setup succeeds, false verification, digest collisions, wrong digest lengths, wrong SHA-family separation, crashes, and timeouts remain hard failures; non-clean invalid-signature CKRs remain xfail evidence. |
| Data-object storage findings | Valid after focused pkcs11-mock rerun and one setup guard. `test_data_objects.py` now treats `CKR_SESSION_COUNT` while opening extra sessions for token-object persistence as a capacity skip. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-data-objects-r2-20260527/` reports 3 passed, 1 skipped, and 9 failed. | The remaining pkcs11-mock rows are real CKO_DATA behavior findings, not setup noise: empty data-object values are rejected, readback returns placeholder values such as `Hello world!` / `Pkcs11Interop`, searches ignore labels, destroyed objects remain findable, and `CKA_CLASS` reads back as empty bytes. |
| Property-fuzz setup classifiers | Valid after regression and focused pkcs11-mock rerun. `test_fuzz.py` now checks AES, digest, RSA, HMAC, and ECDSA mechanisms before Hypothesis-generated operation loops, classifies advertised setup rejects as xfail evidence, and classifies advertised operation rejects as non-clean xfail evidence instead of raw falsifying examples. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-fuzz-r2-20260527/` reports all 11 rows skipped and 0 failed. | Fuzz invariants remain strict once setup and advertised operations succeed: wrong AES roundtrips, digest mismatches, RSA/HMAC/ECDSA verification failures, wrong signature lengths, crashes, and timeouts remain hard findings. |
| Resource/stress setup classifiers | Valid after regression and focused pkcs11-mock rerun. `test_resource.py` and `test_stress.py` now apply the same AES/SHA/RSA setup and operation guards used by the rest of the suite, and treat only exact `CKR_SESSION_COUNT` while opening an additional session as a capacity skip. The focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-resource-stress-r2-20260527/` moved the selected slice from 1 passed / 16 failed to 16 skipped / 1 failed. | The remaining hard row is `test_rapid_random_1000`: pkcs11-mock returns the same 32-byte value for every `C_GenerateRandom` call. That remains a wrong-output/randomness finding. Memory growth checks, wrong crypto outputs after `CKR_OK`, crashes, and timeouts remain strict when setup succeeds. |
| RNG sanity findings | Valid provider/mock behavior after focused pkcs11-mock rerun. `test_rng.py` does not need setup reclassification: `C_GenerateRandom` succeeds, but pkcs11-mock fills output with repeated `0x01` bytes. The focused rerun in `artifacts/_focused/pkcs11-mock-rng-current-20260527/` reports 4 passed and 5 failed. | Keep these as RNG-quality findings: duplicate 32-byte samples, biased bit frequency, single-byte distribution, zero Shannon entropy, and failed runs-test ratio. These rows are useful article evidence that the mock target is synthetic and not a production-quality RNG provider. |
| Quality-audit CTS skip categories | Valid after artifact review. AES-CTS collection keeps CS1/CS2/CS3 vector rows visible as counted skips after a provider-level variant probe. If detection fails, `test_cts_detect.py` is the actual provider finding; if detection succeeds, non-selected CS variants are framework selection skips. The quality audit now classifies those skip reasons as `framework_constraint` instead of `unknown`. | EC/ECDH/XDH import-reject skips intentionally remain `unknown` in quality output for now. A generic "cannot import EC key" CKR may mean unsupported curve, provider import policy, invalid test key material, or another object-template rule, so those buckets need focused provider/source review before being collapsed into a cleaner category. |
| TPM2 interop/crossverify operation rejects | Valid after focused rerun. Current-source `test_interop.py`, `test_crossverify.py`, and `test_crossverify_extended.py` on TPM2 report 23 passed, 4 skipped, 14 xfailed, 0 failed, and 0 crashed. AES/HMAC operations that reach an advertised mechanism and return `CKR_GENERAL_ERROR` are now visible advertised-but-not-operational xfail evidence. | This is not a clean interop result. Exact ciphertext, plaintext, digest, MAC, or signature mismatches after `CKR_OK` remain hard failures. |
| TPM2 ACVP/Wycheproof HMAC runtime rejects | Valid after focused rerun. Current-source TPM2 HMAC rows no longer reproduce the old hard failures: `test_acvp_hmac.py` reports 1,034 skipped and 444 xfailed, while `test_wycheproof_hmac.py` reports 320 passed, 1,214 skipped, and 198 xfailed. The xfails are advertised HMAC mechanisms returning explicit runtime CKRs such as `CKR_GENERAL_ERROR`, not clean passes and not missing-mechanism skips. Focused evidence is stored in `artifacts/_focused/tpm2-hmac-current-20260526/`. | Old TPM2 ACVP/Wycheproof HMAC hard-failure buckets are stale. Article wording should call them advertised-but-not-operational HMAC evidence on current source, while still treating wrong MAC bytes after `CKR_OK`, accepted invalid tags, crashes, and timeouts as hard failures. |
| Message API setup and runtime rejects | Valid after regression and focused pkcs11-mock rerun. Direct v3 message API return checks now keep `CKR_FUNCTION_NOT_SUPPORTED` as an unsupported-function skip, record `CKR_MECHANISM_INVALID` / `CKR_MECHANISM_PARAM_INVALID` and generic runtime CKRs as xfail evidence, and fail unexpected CKRs. AES message tests also use the shared AES keygen setup classifier, so pkcs11-mock's rejected AES setup no longer looks like a message API failure. Focused evidence in `artifacts/_focused/pkcs11-mock-message-api-r2-20260527/` reports 1 passed, 10 skipped, and 0 failed; the immediately previous focused artifact had 5 setup failures. | This does not relax successful operation semantics. Wrong ciphertext/plaintext/signature output after `CKR_OK`, generated parameter writeback bugs, crashes, and timeouts remain hard findings. |
| AES-KWP error-path crash harness | Valid and necessary after focused rerun. The previous subprocess helper accepted positive child exits, and the generated KWP child script had a syntax-path false positive. The decrypt probe also used an oversized output buffer, which hid the OpenSSL `CRYPTO_128_unwrap_pad()` overwrite that only appears with a minimal output buffer. Current OpenCryptoki master + OpenSSL 4.0.0 evidence reports 34 passed and 8 failed KWP rows, all failed rows being KWP decrypt guard-overwrite findings. | Provider artifacts generated before this fix must not be used for AES-KWP crash-survival claims. Use `artifacts/_focused/opencryptoki-master-error-path-current-20260527/` for current article wording. |
| RSA error-path crash harness | Valid after focused rerun. The RSA malformed-ciphertext child scripts had the same generated `try/finally` indentation issue. Current source builds these scripts through a single helper and compiles representative PKCS#1/OAEP variants in meta-tests. Current OpenCryptoki master + OpenSSL 4.0.0 evidence reports all 8 RSA error-path rows passing. | Older provider artifacts that showed this file as fully passing were suspect until rerun; the current focused rerun now supports wording that the corrected OpenCryptoki error-path finding is AES-KWP-specific, not RSA. |
| Subprocess result policy | Valid and should become the default for crash-survival probes. A negative return code is a crash finding, a positive return code is a child-script failure, and only return code 0 means the probe completed. Security helpers, mutex callback probes, the OpenSSL interop wrapper, and `test_fork_after_initialize` now use this rule. The fork test now parses explicit `CHILD_EXIT` / `CHILD_SIGNAL` lines so a child exception or signal cannot be hidden behind a successful parent process. Focused pkcs11-mock evidence in `artifacts/_focused/pkcs11-mock-fork-safety-current-20260527/` reports 1 passed and 0 failed for the fork row. | Remaining bespoke subprocess tests should keep being audited when touched. They are acceptable only if they require `rc == 0`, parse required stdout, or deliberately classify positive exits with explicit xfail/skip wording. |
| Dual-function and operation-state setup guards | Valid after regression and focused pkcs11-mock rerun. `test_dual_function.py` and the same-session state-save rows in `test_operation_state.py` now check required AES/SHA mechanisms in the parent before spawning raw subprocess scripts. Missing setup mechanisms become counted capability skips; advertised-but-broken setup inside the child is still a provider finding. The garbage `C_SetOperationState` negative probe also records `CKR_ARGUMENTS_BAD` as a non-clean compliance note rather than a state roundtrip failure. Current evidence in `artifacts/_focused/pkcs11-mock-dual-operation-r2-20260527/` reports 3 passed, 5 skipped, and 0 failed across the selected dual/state files. | Existing pkcs11-mock hard rows from these files were setup-capability artifacts. This does not weaken crash detection: subprocess signals, positive child exits after setup succeeds, wrong digest/ciphertext/state output, and accepted invalid state remain failures. |
| Remaining-gaps and subprocess-safety setup split | Valid after focused TPM2 rerun. Template-constraint and AES-CMAC straggler rows now use the shared AES setup classifier, so TPM2-style advertised `AES_KEY_GEN` rejects are visible xfail setup evidence. Legacy `C_GetFunctionStatus` / `C_CancelFunction` rows still prefer `CKR_FUNCTION_NOT_PARALLEL`, but `CKR_FUNCTION_NOT_SUPPORTED` is recorded as a compliance note because the general function-list section permits unsupported API stubs. Cross-process session-object isolation now xfails parent `C_CreateObject` setup rejection instead of reporting it as a crash. Current evidence in `artifacts/_focused/tpm2-remaining-sign-safety-r2-20260527/` reports 6 passed, 23 skipped, 8 xfailed, and 1 failed across `test_remaining_gaps.py`, `test_sign_recover.py`, and `test_subprocess_safety.py`. The sign-recover high-level capability probe now treats only an empty token slot list or absent `CKM_RSA_X_509` as unsupported; slot/mechanism enumeration errors propagate as failures. Focused pkcs11-mock evidence in `artifacts/_focused/pkcs11-mock-sign-recover-probe-current-20260527/` reports 6 skipped and 0 failed for the clean unsupported path. | The remaining hard row is still `test_fork_after_initialize` timing out after 15 seconds. That is a subprocess-safety finding for TPM2/daemon behavior, not a setup classification issue. Sign-recover setup rejects are already xfail evidence on current source. |
| CKR operation-state subprocess probes | Valid after correction and focused reruns. `test_encrypt_without_init` now calls raw `C_Encrypt` without `C_EncryptInit`, and the double-digest probe now calls `C_DigestInit` twice instead of using wrapper helpers that reset state. Positive child exits use the shared subprocess-result policy instead of being labelled as crashes. A current TPM2 rerun also showed two wrapper rows failing before the state-machine assertion because AES setup was not operational; those wrappers now use shared setup guards and AES-128 setup keys. Current focused evidence: TPM2 has 3 passed and 2 xfailed setup rows, while OpenCryptoki and SoftHSM2 both have 5 passed and 0 failed for `test_ckr_dual.py`. | Existing provider artifacts that mention `test_ckr_dual.py` subprocess crashes or hard setup failures are pre-fix evidence and should not be used for public crash wording. Other bespoke CKR subprocess files still deserve gradual conversion to the shared classifier. |
| CKR raw/fault subprocess setup classification | Valid after focused reruns. The retained artifacts showed setup assertions in `test_ckr_raw_attrs.py`, `test_ckr_raw_buffer.py`, `test_ckr_raw_state.py`, and `test_ckr_fault_inject.py` reported through `Crash:` wording. These paths now use the shared CKR subprocess classifier: signal exits remain crash findings, positive child exits are child failures, and explicit setup precondition rejects become visible xfail evidence. The same classifier now covers `test_ckr_general.py`, `test_ckr_universal.py`, and `test_ckr_raw_multipart.py`, and the v2.40 no-`C_GetInterfaceList` path emits an `OK` marker. Current evidence: pkcs11-mock reports 29 passed, 16 xfailed, and 0 failed for the selected CKR raw/fault/general slice; TPM2 reports 28 passed, 14 xfailed, and 3 failed in `test_ckr_raw_args_bad.py`. | Old artifact rows from these files should not be described as confirmed provider crashes. Remaining TPM2 hard rows are current target findings: `C_DigestInit(NULL)` signal 11 and `CKR_FUNCTION_NOT_SUPPORTED` from `C_GenerateKey(NULL)` / `C_WrapKey(NULL)`. `C_GenerateKey(NULL)` now requires `CKR_ARGUMENTS_BAD`; key generation has no NULL-mechanism cancellation success path. |
| TPM2 ACVP AES-CFB128 runtime rejects | Valid after focused rerun. Current-source `test_acvp_aes_cfb128.py` on TPM2 no longer reports the old hard-failure bucket: every selected row reaches advertised `CKM_AES_CFB128` and returns `CKR_GENERAL_ERROR`, which is visible advertised-but-not-operational xfail evidence. Focused rerun evidence is stored in `artifacts/_focused/tpm2-cfb128-current-20260526/`. | This is still a provider finding, not a clean pass. Exact ciphertext/plaintext mismatches after `CKR_OK`, crashes, and timeouts would remain hard failures. Full matrix artifacts generated before the classifier patch still contain stale hard failures for this file. |
| Security crash-probe setup preflight | Valid and needed. API-boundary, arithmetic-overflow, FFI length-boundary, RSA error-path, and NULL-pointer probes now preflight setup key generation in the parent before entering the crash-isolated child. Arithmetic-overflow and FFI length-boundary child scripts also classify explicit AES/RSA/EC setup rejects with `SETUP_XFAIL:` before the malformed FFI call, so TPM2-style child keygen rejects do not look like probe failures. Focused null-pointer reruns did not expose hidden HMAC/import setup failures in the sampled providers: TPM2 has 16 passed, 3 skipped, 8 AES setup xfailed, and 0 failed; OpenCryptoki has 27 passed; SoftHSM2 has 23 passed and 4 skipped. A focused arithmetic-overflow rerun now shows the old TPM2 hard bucket is stale setup-classification evidence: current TPM2 reports 18 passed, 6 skipped, 15 xfailed, and 0 failed. The same arithmetic-overflow rerun still leaves current provider crash/abort findings for SoftHSM2, OpenCryptoki, and Kryoptic main. Signal exits, panics, timeouts, positive child exits from malformed calls, and wrong results from the actual malformed call remain failures. | Existing FFI length-boundary artifact rows should use the focused current reruns below rather than older matrix rows. Remaining FFI failures after setup correction are provider boundary findings, not candidates for skip/xfail. |
| SoftHSM2 FFI length-boundary rerun | Valid provider crash/failure evidence after focused rerun. Current-source stock SoftHSM2 2.7.0 reports 14 passed, 6 skipped, 1 xfailed, and 5 failed in `artifacts/_focused/softhsm2-ffi-length-current-20260526/`. The hard failures include the confirmed `C_EncryptInit(CKM_AES_GCM, pIv=NULL, ulIvLen=12)` signal 11 and four positive child exits for huge `ulDataLen` HMAC/SHA256 `C_Sign`/`C_Digest` probes. The local generated-IV simulator target historically removes only the GCM null-IV crash by adding a `pIv == NULL_PTR` guard. | Keep these as visible security-boundary findings. A positive child exit is not a signal crash, but the shared subprocess policy correctly treats it as a failed probe rather than success. If an upstream report needs source-level root cause, collect a debugger stack trace next; no pkcs11-check skip/xfail should be added. |
| FFI length-boundary current reruns | Valid after focused reruns and one setup fix. `test_ffi_length_boundary.py::TestEddsaNullContext` now uses `CKM_EC_EDWARDS_KEY_PAIR_GEN` for Ed25519 setup instead of generic `CKM_EC_KEY_PAIR_GEN`; the pre-fix OpenCryptoki/NSS/Kryoptic EdDSA setup failures were harness noise. Current focused evidence: TPM2 reports 9 passed, 9 skipped, 6 xfailed, 2 failed; OpenCryptoki 3.27.0 reports 15 passed, 6 skipped, 5 failed; Kryoptic main reports 19 passed, 7 failed; NSS main reports 15 passed, 8 skipped, 3 failed; pkcs11-mock reports 1 passed, 25 skipped, 0 failed. | Article wording can use these focused FFI rows as current crash-boundary evidence for the sampled providers. Remaining failures are signal exits or timeout/positive-child failures from the intended malformed call path: TPM2 digest length, OpenCryptoki sign/digest plus ML-DSA explicit-empty-context abort, Kryoptic sign/digest plus AES large-value timeout and KDF NULL-pointer crashes, and NSS sign length plus GCM NULL-IV crash. |
| NULL-pointer PIN/token scripts | Valid after focused reruns. `C_SetPIN` and `C_InitToken` child probes now pass valid PIN/label buffers as `CK_UTF8CHAR_PTR`, matching the raw binding signature. The old positive subprocess exits were Python `ctypes` type errors, not provider findings. Current focused `test_ffi_null_pointer.py` evidence has no hard failures or crashes for TPM2, OpenCryptoki, or SoftHSM2. | Old `test_ffi_null_pointer.py` artifact failures from before the signature fix should not be used as provider findings. |
| Zero-length AES-CBC generated child script | Valid. The AES-CBC zero-length API-boundary child script now compiles in meta-tests and preflights AES setup before spawning. | Older target artifacts that failed this row should be treated as pre-fix harness noise. |
| Dataset coverage audit | Valid as an analysis artifact. It inventories local ACVP, Wycheproof, CCTV, and x509-limbo data and lists useful gaps without enabling unreviewed tests. | Follow-up tests should be added only when PKCS#11 can actually express the vector input. |
| X.509 limbo stress import boundary | Valid after tightening. A provider may reject malformed Limbo material during `C_CreateObject`, but arbitrary Python exceptions in the harness should not be swallowed as acceptable rejects. The stress tests now only treat `AssertionError` from raw CKR helpers as import rejection. | Provider artifacts that predate the X.509 catch-boundary patches should not be treated as final article evidence without a current rerun. |
| Docker provider exit propagation | Fixed after regressions. The focused pkcs11-mock X.509 rerun produced failed units in `results.json`, but the Docker target exited successfully because `docker/pkcs11-mock/Dockerfile` appended `|| true` after `run-pkcs11-check.sh`. The focused TPM2 RSA-PSS rerun exposed the same class of issue in `docker/tpm2-pkcs11/run-tpm2.sh`, where an `if ! run-pkcs11-check.sh` wrapper printed a note and then exited successfully. `tests/test_docker_targets.py` now scans provider Dockerfiles and shell wrappers for both masking patterns, and current pkcs11-mock/TPM2 wrappers propagate the real pkcs11-check exit code. | Older pkcs11-mock and TPM2 Docker shell exit statuses are not reliable evidence. Use `results.json` for those artifacts, and rerun current Docker targets when an exit-code gate matters. |
| X2RATCHET Montgomery setup | Valid after regression. The local PKCS#11 Double Ratchet text permits curve 255 or 448, so `test_double_ratchet.py` no longer probes advertised X2RATCHET paths with classic EC/P-256 setup. It now requires Montgomery key generation for setup, tries X25519 and then X448, classifies only explicit setup CKRs, and lets Python-side errors propagate. A focused pkcs11-mock artifact is stored in `artifacts/_focused/pkcs11-mock-double-ratchet-current-20260527/`; the mock has no X2RATCHET mechanism coverage, so the slice remains capability-skip evidence rather than provider behavior evidence. | Old X2RATCHET rows produced by the P-256 fallback should not be used as spec-faithful Double Ratchet evidence. Providers that advertise X2RATCHET still need a focused rerun to separate missing Montgomery setup, advertised-but-not-operational CKRs, and real derive/encrypt/decrypt behavior. |
| `C_SetPIN` catch boundary | Valid after regression. The destructive `test_so_pin.py` PIN-change row no longer converts arbitrary Python exceptions or generic runtime CKRs into skips. Exact token-policy/permission CKRs remain skips, generic `CKR_DEVICE_ERROR` / `CKR_FUNCTION_FAILED` / `CKR_GENERAL_ERROR` become visible xfail evidence, and local setup bugs propagate. A focused pkcs11-mock artifact is stored in `artifacts/_focused/pkcs11-mock-so-pin-current-20260527/`; the default run is destructive-gated, so it confirms gating rather than provider PIN-change behavior. | Future destructive provider runs can use this row for PIN-change evidence, but normal matrix skips should not be described as a provider `C_SetPIN` result. |
| `C_InitPIN` access-level catch boundary | Valid after regression. `test_access_levels.py::TestSOSessionCapabilities::test_so_can_init_pin` now validates `C_Login(CKU_SO)` after the explicit SO-PIN/conflict skips and no longer catches arbitrary Python exceptions as "C_InitPIN not supported". Exact token-policy or environment CKRs remain skips, generic runtime rejects such as `CKR_FUNCTION_FAILED` become visible xfail evidence, and local setup bugs propagate. A focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-access-levels-init-pin-current-20260527/` reports 27 skipped and 0 failed because the default run is still destructive/capability gated. | The focused mock artifact is not provider `C_InitPIN` behavior evidence. Future destructive runs should keep SO-login failures, `C_InitPIN` runtime CKRs, and successful PIN restoration separate in the report. |
| `C_GetObjectSize` catch boundary | Valid after regression. `test_object_size.py` no longer treats arbitrary exceptions or generic `C_GetObjectSize` CKRs as "unsupported"; only `CK_UNAVAILABLE_INFORMATION` or zero-size successful replies map to the existing skip path. Generic runtime CKRs such as `CKR_FUNCTION_FAILED` become visible xfail evidence, and local bugs propagate. A focused pkcs11-mock rerun in `artifacts/_focused/pkcs11-mock-object-size-current-20260527/` reports 2 skips and 1 hard failure: both 100-byte and 10KB data objects report size 256. | The pkcs11-mock object-size row is a real wrong-result finding after `CKR_OK`, not a harness setup issue. Future article wording should keep object-size semantics separate from object-value readback findings. |
| Authenticated-wrap runtime and integrity boundaries | Valid after regression. `test_authenticated_wrap.py` no longer turns baseline wrap-side `AssertionError`/CKR rejects into skips after the v3.2/AES-GCM or AES-KEY-WRAP preconditions have passed. Those advertised-but-rejected wrap operations now become visible xfail evidence for specific runtime CKRs. The AES-GCM tampered-tag and ciphertext-bitflip checks also only accept specific integrity-rejection CKRs plus documented module quirks; arbitrary Python/recipe assertion bugs propagate as real harness failures. The ECDH-AES-KW rows now use the shared EC keypair setup classifier, so missing EC keygen skips and advertised EC keygen CKR rejects xfail. Focused pkcs11-mock runs in `artifacts/_focused/pkcs11-mock-authenticated-wrap-current-20260527/` and `artifacts/_focused/pkcs11-mock-ecdh-aes-kw-current-20260527/` skip because the mock lacks the relevant capability/interface surface. | The focused mock artifacts are not AES-GCM authenticated-wrap or ECDH-AES-KW behavior evidence. Providers with v3.2 authenticated wrap or ECDH-AES-KW support should be rerun so article wording can distinguish unsupported API/setup, advertised-but-not-operational wrap CKRs, accepted tampering after `CKR_OK`, and local harness errors. |
| Attribute-default setup classifiers | Valid after regression. `test_attribute_defaults.py` no longer turns AES/RSA key-generation `AssertionError` paths into broad fixture skips. Secret-key defaults now use an AES-128 setup key because default-attribute semantics do not depend on AES-256 support, and both AES and RSA setup use the shared key-generation classifiers: missing mechanisms skip, advertised runtime rejects xfail, and local bugs propagate. A focused pkcs11-mock run in `artifacts/_focused/pkcs11-mock-attribute-defaults-current-20260527/` reports 10 skipped, 12 xfailed, and 0 failed. | The pkcs11-mock artifact mostly reflects missing AES keygen and existing malformed/default attribute xfails; it is not a clean pass for all default-attribute semantics. Wrong defaults after successful setup remain xfail/failure findings depending on the documented compliance level. |
| Access-level trusted-setattr setup boundary | Valid after regression. `test_access_levels.py::TestTrustedAttribute::test_user_cannot_setattr_trusted` no longer catches every setup `AssertionError` from the shared AES setup helper as a skip. Known setup outcomes remain classified by `_gen_access_aes_key`; unexpected harness/setup assertions now propagate as real failures. A focused pkcs11-mock run in `artifacts/_focused/pkcs11-mock-access-trusted-setattr-current-20260527/` reports one skipped row and 0 failed. | The mock row is capability/setup evidence only. The actual security finding remains whether a USER session can set `CKA_TRUSTED=True` after a baseline key is successfully created. |

## Provider-Result Interpretation

The result table for the article should avoid a single "best provider" ranking.
A better interpretation is:

- "passed" means the provider produced the expected behavior for the tested
  PKCS#11-visible operation;
- "xfailed" means the suite saw a known or classified non-clean provider result,
  not a pass;
- "skipped" mixes missing mechanisms, unsupported interface versions, and exact
  duplicate vector encodings, so it needs a short explanation in any public
  table;
- "crashed" and "timeout" are first-class findings, even if the rest of a
  provider has good mechanism coverage.

## Required Before Commit

- Finish the current provider rerun or stop it with an explicit partial-results
  note.
- Compare every completed target with the baseline snapshot and update the
  rerun note.
- Run focused regression tests, `ruff`, full meta-tests, `mypy`, and
  `git diff --check` after the final edit.
- Commit only source/docs/tests/artifacts intentionally changed for this work;
  leave unrelated `.claude/` untracked.
